"""функция is_between(x, y, z), которая
возвращает True, если истинно выражение 
x ≤ y ≤ z, и False в противном случае."""

def is_between(x, y, z):
    return x <= y <= z

print(is_between(2, 4, 4))